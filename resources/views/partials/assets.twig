{#
    Main CSS can be found in metadata.twig
#}
{{ asset_script("pyrocms.theme.afterburner::js/theme.js", ["min", "watch@pyrocms.theme.afterburner::js/"]) }}

{# Include Scripts Loaded From Outside #}
{% for script in asset_scripts("scripts.js", ["min"]) %}
    {{ script|raw }}
{% endfor %}

{# Deferred JS Includes #}
{{ template.includes.render('scripts')|raw }}

{# Include Styles Loaded From Outside #}
{% for style in asset_styles("styles.css", ["min"]) %}
    {{ style|raw }}
{% endfor %}
